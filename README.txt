Follow the steps:

1) open the cmd
2) go to the folder containing index.js
3) run "npm init"
4) run "npm i express"
5) run "npm i nodemon"
6) run "npx nodemon index.js"
7) go to broswer
8) type "localhost:3000"