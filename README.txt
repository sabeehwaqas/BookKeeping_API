Follow the steps:

1) download the folder
2) extract the folder
3) open the cmd
4) go to the folder containing index.js
5) run "npm init"
6) run "npm i express"
7) run "npm i nodemon"
8) run "npx nodemon index.js"
9) go to broswer
10) type "localhost:3000"
